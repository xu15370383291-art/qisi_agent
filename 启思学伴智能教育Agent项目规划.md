# 启思学伴智能教育 Agent 项目落地规划

> 项目定位：面向中小学生的个性化学习辅助 Agent，融合 LangGraph Agent 编排、RAG 知识检索、分层记忆和 GraphRAG 知识关联，为学生提供可追溯的答疑、错题分析和学习建议，并为教师提供学情分析依据。
>
> 本期边界：聚焦单学科、单年级 MVP 的产品和技术实现；本规划暂不包含生产环境部署、上线运维和大规模多租户运营。

## 1. 项目背景与痛点

学生学习基础和节奏差异明显，统一进度和固定题目难以覆盖个体需求。教材、课程资料、题库和错题记录分散，学生遇到问题时难以快速定位相关知识点及前置概念。错题往往只被记录，缺少错误类型、薄弱知识点和后续复习路径的结构化分析。教师需要人工批改、整理错题和跟踪班级学情，个别辅导成本较高。普通大模型回答缺少稳定的教材依据，也无法持续利用学生历史学习状态，存在幻觉和重复辅导问题。

## 2. 建设目标与项目范围

### 2.1 建设目标

- 建立教材、课程资料、题库和错题资料的统一知识服务能力。
- 通过 Agent 路由和专业子 Agent，完成答疑、错题分析、出题和学情分析等任务协同。
- 通过短期记忆、长期语义记忆和知识图谱实现持续、个性化的上下文增强。
- 保证回答可追溯、知识库外问题可拒答、Agent 状态可恢复，并为教师提供可解释的分析结果。

### 2.2 首期范围

| 范围 | 首期内容 | 暂不纳入 |
|---|---|---|
| 学生端 | 教材问答、错题讲解、知识点查询、学习记录 | 复杂社交和直播课堂 |
| 教师端 | 班级薄弱知识点和错题分布查看 | 完整教务系统和排课 |
| Agent | 答疑、错题、出题、学情四类 Agent | 开放式自主工具市场 |
| 数据 | 单学科、单年级、受控教育语料 | 跨学校大规模数据治理 |

## 3. 用户角色与核心场景

- **学生**：提交文字、图片或题目，获得分步骤讲解、错因分析、相似题练习和复习建议。
- **教师**：查看班级知识点掌握情况、错题聚类和重点辅导学生，并可对 Agent 结果进行人工修正。
- **系统管理员**：维护教材、题库、知识点和评测集，查看检索、模型调用和异常日志。

典型闭环：

```text
学生提问
  → 意图识别
  → 记忆与知识召回
  → 专业 Agent 处理
  → 流式输出答案与引用
  → 提取学习事件
  → 更新学生画像和长期记忆
```

## 4. 总体系统架构

```text
学生端 / 教师端（Vue3 + TypeScript）
                │
       FastAPI API 层（鉴权、校验、SSE）
                │
      LangGraph Supervisor / StateGraph
                │
答疑 Agent │ 错题分析 Agent │ 出题 Agent │ 学情分析 Agent
                │
检索服务 │ 记忆服务 │ GraphRAG 服务 │ 教育工具服务
                │
PostgreSQL + pgvector │ Redis │ Neo4j │ 对象存储 │ 模型服务
```

- **交互层**：负责问题提交、流式状态展示、答案引用和学情看板。
- **服务层**：FastAPI 提供统一接口，SSE 推送节点状态、Token 增量、工具调用摘要、引用和完成事件。
- **编排层**：Supervisor Agent 负责意图识别、任务拆解和子 Agent 调度，StateGraph 管理共享状态、分支和检查点。
- **能力层**：RAG、长期记忆、知识图谱和教育工具以独立服务或工具节点接入，避免全部逻辑堆叠在单一 Prompt 中。
- **数据层**：PostgreSQL 保存业务实体和学习事件，pgvector 保存语义向量，Redis 保存会话状态，Neo4j 保存教育知识关系。

## 5. Agent 流程编排设计

- **分级路由**：采用规则/关键词 → 向量意图匹配 → LLM 推理的分级路由，简单问题优先走低成本路径，复杂问题再进入模型判断。
- **专业分工**：答疑 Agent 负责知识解释，错题 Agent 负责错误归因，出题 Agent 负责难度控制和题目生成，学情 Agent 负责掌握度和复习建议。
- **状态管理**：在 StateGraph 中维护 `user_id`、`session_id`、`query`、`memory_hits`、`retrieval_hits`、`tool_results`、`citations`、`answer` 和 `confidence` 等字段。
- **并发与流式**：记忆召回、向量检索和图谱扩展通过异步任务并行执行；SSE 分阶段推送“分析中、检索中、生成中、完成”等状态。
- **可恢复执行**：使用 LangGraph Checkpoint 保存节点状态和中间结果，支持模型异常、人工介入或用户中断后的断点恢复。
- **人工介入**：涉及成绩修改、学习计划确认或低置信度结论时进入 HITL 节点，由教师确认后继续流程。

## 6. RAG 知识检索设计

- **文档处理**：解析教材、课程资料、题库和错题文档，建立 `document_id`、`chapter_id`、`question_id`、`knowledge_point_id` 等可追踪标识。
- **切分策略**：教材采用标题感知和语义边界切分；题库按题号、题干、选项、答案、解析、难度和知识点结构化切分；控制 Chunk 长度并保留课程、章节、题型和来源元数据。
- **语义召回**：使用 Embedding 模型生成向量，基于 PostgreSQL + pgvector 执行余弦相似度检索，并按学生、课程和知识点进行元数据过滤。
- **词法召回**：使用 BM25 处理题号、公式、缩写和专业术语等精确匹配场景；对多路候选结果采用加权 RRF 融合和 Top-K 截断。
- **上下文组装**：将检索片段、来源信息和必要的记忆结果组装为受控上下文，限制上下文长度和重复内容。
- **生成控制**：设置相似度阈值，知识库外问题触发拒答或转人工；每个回答保留文档、章节、题目或来源路径，支持依据回溯。
- **评测边界**：Hit@K、Recall@K、MRR 只用于知识库检索质量评估；正式指标必须基于教育领域标注集重新统计，不能直接套用其他领域数据。

## 7. 分层记忆系统

### 7.1 记忆分层

- **短期记忆**：Redis 保存当前会话、多轮上下文和临时 Agent 状态，并设置 TTL 避免上下文无限增长。
- **业务记忆**：PostgreSQL 保存学生画像、学习目标、错题事件、知识掌握度和学习偏好。
- **长期语义记忆**：重要学习事件不直接保存完整聊天记录，而是抽取为结构化记忆后生成 Embedding，写入长期记忆向量空间。
- **知识图谱记忆**：Neo4j 保存学生、课程、章节、知识点、题目和错因之间的关系，用于结构化推理和 GraphRAG 扩展。

### 7.2 长期记忆写入

通过 LLM Structured Output 抽取学习事件，使用 Pydantic 约束以下字段：

```text
memory_type、student_id、course_id、knowledge_point_id、content、
confidence、importance、occurred_at、source_message_id、version
```

写入流程：

```text
对话事件
  → 学习事件识别
  → Pydantic Schema 校验
  → 实体归一化
  → 相似度去重
  → 冲突检测与版本判断
  → 重要性评分与时间衰减
  → Embedding 生成
  → PostgreSQL + pgvector Upsert
```

长期记忆携带 `student_id`、`course_id`、`knowledge_point_id`、时间、来源和置信度等元数据，与教材知识库使用独立命名空间，避免不同类型内容混淆。

### 7.3 记忆增强检索

用户提问时，系统先判断是否需要历史信息，再并行召回长期记忆和教材知识。召回结果按照学生、课程和知识点进行过滤，并综合向量相似度、知识点匹配度、时间衰减和记忆重要性排序，最后通过 RRF 或加权融合注入 Agent 上下文。

记忆生命周期采用：

```text
candidate → active → updated / expired / deleted
```

支持版本追踪、来源回溯、主动删除、教师修正和冲突覆盖。

## 8. 知识图谱与 GraphRAG

GraphRAG 作为后续知识扩展层，在基础 RAG 和记忆系统稳定后接入。首期先完成实体关系模型和离线验证，不把尚未实现的图谱能力写成已完成成果。

### 8.1 图谱模型

```text
学生 ──掌握──> 知识点
学生 ──错答──> 题目
题目 ──考查──> 知识点
题目 ──属于──> 章节
知识点 ──依赖──> 前置知识点
题目 ──相似──> 题目
```

### 8.2 GraphRAG 查询链路

```text
Query 改写
  → 实体识别与知识点对齐
  → pgvector / BM25 初召回
  → Neo4j Cypher 图谱扩展
  → 前置知识点、关联题目和相似错题补充
  → 长期记忆融合
  → 加权排序与上下文组装
  → 生成学习建议
```

实体和关系通过结构化输出抽取，经实体归一、置信度校验和冲突处理后写入 Neo4j。GraphRAG 重点用于知识点依赖分析、错题关联和学习路径生成。

## 9. 数据模型与接口约定

| 数据域 | 核心实体/字段 | 用途 |
|---|---|---|
| 用户与课程 | `users`、`courses`、`student_profiles` | 身份、年级、课程范围和学习画像 |
| 学习事件 | `learning_events`、`wrong_answers`、`mastery_records` | 记录错题、掌握度和学习行为 |
| 知识库 | `documents`、`knowledge_chunks`、`embeddings` | 文档、Chunk、向量和来源追踪 |
| 记忆 | `memory_items`、`memory_versions` | 长期记忆、置信度、重要性和生命周期 |
| 图谱 | `Student`、`KnowledgePoint`、`Question`、`Chapter` | 实体关系和 GraphRAG 扩展 |

核心接口规划：

- `POST /api/chat/stream`：提交问题并通过 SSE 返回流式事件。
- `GET /api/conversations/{id}`：读取会话状态和消息记录。
- `GET /api/retrieval/debug`：查看检索候选、分数、来源和过滤条件。
- `GET /api/students/{id}/learning-profile`：查看学生画像和知识掌握情况。
- `POST /api/memories/{id}/review`：教师确认、修正或撤销长期记忆。

SSE 事件类型：`node_started`、`retrieval`、`memory_hit`、`tool_call`、`answer_delta`、`citation`、`human_review`、`error`、`done`。

## 10. 工具治理、可靠性与安全

- 统一 `ToolSpec` 契约，声明工具名称、输入 Schema、权限范围、超时、重试、限流、幂等性和副作用等级。
- 按注册层、选择层和执行层管理工具：先按用户角色过滤，再按意图选择工具，最后执行超时、重试、熔断和结果校验。
- 对学生数据实施 `student_id` 隔离、JWT 鉴权、字段级权限和敏感信息脱敏。
- 长期记忆写入增加幂等键，避免重复记录；写操作和成绩相关操作进入教师确认节点。
- 建立 Trace ID、节点耗时、检索命中、模型调用和错误原因日志，支持按请求回放 Agent 执行链路。

## 11. 分阶段实施路线

### 阶段 0：数据与评测基线

确定单学科、单年级范围，整理教材、题库、错题样本，定义知识点体系、数据 Schema 和教育领域评测集，形成检索基线报告。

### 阶段 1：RAG MVP

完成文档解析、结构化切分、Embedding、PostgreSQL + pgvector 检索、BM25 召回、RRF 融合、来源引用、知识库外拒答和 SSE 流式问答，交付可运行的答疑与错题讲解链路。

### 阶段 2：Agent 编排与长期记忆

接入 Supervisor Agent 和答疑、错题、出题、学情四类子 Agent；完成 StateGraph 状态管理、Checkpoint、短期会话记忆、学习事件抽取、长期记忆写入和历史错题召回。

### 阶段 3：知识图谱与 GraphRAG

接入 Neo4j，完成实体关系抽取、实体归一、Cypher 扩展和向量结果融合，验证知识点前置依赖、关联题目和学习路径分析。

### 阶段 4：教师端与验收优化

补充教师端学情视图、人工审核、检索调试、异常治理和完整测试，基于试用反馈修正数据、Prompt、路由和记忆策略。

## 12. 测试与验收指标

以下为本项目规划目标，不代表尚未完成的实际测量结果；正式验收需要使用教育领域真实标注集和固定测试环境重新执行。

### 12.1 RAG 知识库指标

- `Recall@5` ≥ 85%。
- `MRR` ≥ 80%。
- 知识库外问题拒答准确率 ≥ 90%。
- 引用与检索来源关联率达到 100%。

Hit@K、Recall@K 和 MRR 只评估教材/题库知识检索，不用于证明长期记忆质量。

### 12.2 长期记忆指标

- 建立历史错题回溯、学习偏好召回和多轮上下文关联测试集。
- 评估 `Memory Recall@K`、记忆更新成功率、记忆冲突率和重复写入率。
- 目标：重复写入率 ≤ 1%，记忆来源可追溯率 100%。

### 12.3 GraphRAG 与 Agent 指标

- 实体和关系抽取准确率 ≥ 85%。
- 图谱扩展结果的有效知识点占比 ≥ 80%。
- Agent 任务路由正确率 ≥ 90%。
- 首 Token 延迟 ≤ 3 秒，常规请求端到端响应时间 ≤ 8 秒。
- 关键流程具备 Checkpoint 恢复能力，工具调用失败可重试或转人工。

## 13. 推荐技术栈

- **Agent 编排**：Python、LangGraph、LangChain、Pydantic、asyncio。
- **模型服务**：Qwen、DeepSeek 或 GPT；Embedding 模型；LLM Structured Output。
- **后端接口**：FastAPI、SSE、HTTPX、JWT。
- **检索与记忆**：PostgreSQL、pgvector、BM25、RRF、Redis。
- **知识图谱**：Neo4j、Cypher、GraphRAG。
- **前端应用**：Vue3、TypeScript、Element Plus。
- **数据处理**：PyMuPDF、python-docx、Markdown 解析、Pandas。
- **测试与审计**：pytest、结构化日志、Trace ID、Prometheus/Grafana。

## 14. 风险与控制措施

- **知识库质量不足**：建立文档规范、版本字段和人工抽检流程，未经审核的资料不进入正式索引。
- **模型幻觉**：通过 RAG 引用、相似度阈值、结构化输出和知识库外拒答限制生成范围。
- **记忆污染**：记忆写入经过置信度、冲突和来源校验，并提供撤销、修正和版本回溯能力。
- **系统延迟**：检索与记忆并行执行，减少重复 LLM 调用，使用流式事件改善等待感知。
- **范围失控**：以单学科、单年级 MVP 为边界，GraphRAG 和复杂教师能力按阶段验收。

## 15. 最终交付物

1. 一套可运行的教育 Agent MVP，支持学生端问答和错题讲解。
2. 一套教材、题库和错题知识库处理及 RAG 检索流程。
3. 一套分层记忆服务，支持学习事件抽取、长期记忆写入、召回和修正。
4. 一套 LangGraph Agent 编排流程，包含子 Agent、SSE 事件和 Checkpoint。
5. 一套 Neo4j 知识图谱和 GraphRAG 验证能力。
6. 一套离线评测集、自动化测试、指标报告和问题迭代清单。

## 16. 落地原则

整体实施顺序保持为：

```text
教育痛点
  → 个性化学习目标
  → Agent 任务编排
  → RAG 提供教材依据
  → 长期记忆提供学生上下文
  → GraphRAG 提供知识关系
  → 生成可追溯的个性化回答
  → 记录反馈并更新记忆
```

先完成可测量的基础 RAG 问答，再增加长期记忆和多 Agent，最后扩展 Neo4j 与 GraphRAG。每个阶段均保留可运行版本、测试数据和评测报告，避免在缺少真实数据和指标的情况下过早堆叠复杂能力。
